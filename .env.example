# Copy this file to .env and fill in the values

# Deployed GAS web app URL
GAS_URL=https://script.google.com/macros/s/REPLACE_WITH_YOUR_DEPLOYMENT_ID/exec

# Shared API key — used by the frontend (as a password), FastAPI middleware, and GAS.
GAS_API_KEY=your_raw_api_key_here
